
## 🅰️
```dataviewjs
const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("A"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));
```

## 🅱️ 
```dataviewjs
const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("B"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", ")); 
```
## 🅲 
```dataviewjs
const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("C"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));
```
## 🅳 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("D"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", ")); 
```
## 🅴 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("E"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", ")); 
```
## 🅵 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("F"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅶 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("G"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅷 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("H"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅸 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("I"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅹 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("J"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅺 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("K"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅻 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("L"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅼 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("M"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅽 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("N"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));  
```
## 🅾️ 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("O"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🅿️ 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("P"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆀 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("Q"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆁 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("R"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆂 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("S"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆃 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("T"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆄 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("U"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆅 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("V"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆆 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("W"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆇 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("X"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆈 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("Y"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
## 🆉 
```dataviewjs


const pages = dv.pages('"Education/English Vocabulary"')
  .where(p => p.file.name.startsWith("Z"))
  .sort(p => p.file.name);
dv.paragraph(pages.map(p => `[[${p.file.name}]]`).join(", "));   
```
